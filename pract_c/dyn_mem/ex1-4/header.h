#ifndef IMP_H
#define IMP_H

#include <stdio.h>
#include <stdlib.h>

int arr_input(int*, int);
int sum (int*, int);
float average(int*,int);
int max_output(int*, int);
int min_output(int*, int);
void reverse(int*, int);
int* arr_fill(int*,int);

#endif